import Navbar from "./components/Navbar";

export default function Home() {
  return (
    <main className="bg-[#0D1117] text-white min-h-screen">
      {/* Navbar */}
      <Navbar />

      {/* Add padding at top so content doesn't sit under navbar */}
    </main>
  );
}
